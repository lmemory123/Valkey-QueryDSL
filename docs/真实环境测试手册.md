# 真实环境测试手册

更新时间：2026-04-04

本文档用于统一 `Valkey QueryDSL` 的测试原则与本地验证方式。

## 一、测试原则

项目当前采用“真实 Valkey 环境优先”的测试策略。

约束如下：

- 正确性结论以真实 Valkey 集成测试为准
- 不使用 Mockito 作为运行时行为正确性的主要依据
- standalone、read_write_split、cluster 三种拓扑都必须有真实验证
- 纯模型 / 纯 DSL / 纯编译期测试只用于补充静态回归，不作为运行时正确性结论

额外红线：

- 没有真实环境验证兜底的运行时功能，不算真正交付
- 不能假设本地已有索引、已有数据或已有 cluster 状态在 CI fresh 容器里仍然成立
- 改动一旦影响索引、路由、批量写、聚合、Facet、向量查询，必须重跑受影响拓扑的真实测试

## 二、本地环境约定

当前默认本地端口如下：

- standalone：`localhost:6379`
- read_write_split：
  - primary：`localhost:6381`
  - replica：`localhost:6382`
- cluster：
  - seed：`localhost:8000`
  - node2：`localhost:8001`
  - node3：`localhost:8002`

CI 与远端基线镜像：

- `ghcr.io/lmemory123/valkey-bundle:9.1.0`
- 这是当前仓库真实验证所依赖的 bundle 镜像，包含项目实际使用的 Search/JSON 能力组合

## 三、真实测试命令

### 1. Standalone

```bash
env VALKEY_INTEGRATION=true /tmp/apache-maven-3.9.11/bin/mvn -B -ntp -pl valkey-query-test-example -am \
  -Dtest=EnableValkeyQueryIntegrationTests,GlideValkeyRepositoryIntegrationTests,BulkWriteIntegrationTests,IndexManagementIntegrationTests,SkuAdvancedIntegrationTest,SkuDeserializationTest,ValkeyComparisonTests,ProjectionIntegrationTests,AggregateIntegrationTests,VectorIntegrationTests \
  -Dsurefire.failIfNoSpecifiedTests=false test
```

### 2. Read / Write Split

```bash
env VALKEY_RW_INTEGRATION=true /tmp/apache-maven-3.9.11/bin/mvn -B -ntp -pl valkey-query-test-example -am clean \
  -Dtest=ReadWriteSplitIntegrationTests,ReadWriteSplitIndexManagementIntegrationTests,ReadWriteSplitVectorIntegrationTests \
  -Dsurefire.failIfNoSpecifiedTests=false test
```

### 3. Cluster

```bash
env VALKEY_CLUSTER_INTEGRATION=true /tmp/apache-maven-3.9.11/bin/mvn -B -ntp -pl valkey-query-test-example -am \
  -Dtest=ClusterBulkWriteIntegrationTests,ClusterAggregateIntegrationTests,ClusterIndexManagementIntegrationTests,ClusterVectorIntegrationTests \
  -Dsurefire.failIfNoSpecifiedTests=false test
```

## 四、最近一次真实验证结果

最近一次在本地 Docker Valkey 环境上的结果：

- standalone：`Tests run: 31, Failures: 0, Errors: 0, Skipped: 0`
- read_write_split：`Tests run: 21, Failures: 0, Errors: 0, Skipped: 0`
- cluster：`Tests run: 23, Failures: 0, Errors: 0, Skipped: 0`

也可以直接使用仓库脚本：

```bash
./scripts/run-real-tests.sh standalone
./scripts/run-real-tests.sh rw
./scripts/run-real-tests.sh cluster
./scripts/run-real-tests.sh all
```

说明：

- `run-real-tests.sh` 当前会先执行一次 `clean install`
- 这是为了确保注解处理器和生成类不会拿到旧产物，尤其是包含 APT 变更时
- 如果改动涉及 APT、向量字段或 Query 生成，统一以脚本结果为准，不要跳过 bootstrap 直接跑单模块

## 五、已确认的真实环境差异

以下问题都不是 mock 测试能稳定发现的：

- 副本节点可能没有 Search 索引元数据，`FT.*` 读请求需要回退主库
- cluster 更适合使用单 seed 真实接入，不应在测试里伪造多 seed 透明接入
- 默认索引治理模式为 `NONE` 时，集成测试必须显式建索引
- standalone bulk 用例在 fresh 容器里也必须显式 `checkAndCreateIndex()`，不能依赖本地已有 `idx:sku`
- macOS Docker Desktop 下，本地 3 节点 cluster 推荐使用 `host.docker.internal` 作为 `cluster-announce-ip`，否则 cluster bus 容易因为 `127.0.0.1` 回环导致 `cluster_state:fail`
- `COUNT_DISTINCT` 当前真实服务端只按“带 `GROUPBY` 的 reducer”对外承诺，cluster 下不支持
- `countDistinct(field)` 当前只对单字段精确计数对外承诺，底层会顺序分页遍历 distinct 结果
- `facetChain.minCount(...)` 当前按客户端安全过滤实现，对 standalone / read_write_split / cluster 三种拓扑都已有真实验证
- `facetChain.includeValues(...)` 当前按客户端安全过滤实现，对 standalone / read_write_split / cluster 三种拓扑都已有真实验证
- 向量 KNN 当前已在 standalone / read_write_split 下真实跑通，在 cluster 下已真实验证为当前不支持

## 六、后续规则

后续新增功能时，至少满足以下要求：

1. 如果改动影响 Repository 运行时行为，必须补真实 Valkey 集成测试
2. 如果改动影响拓扑路由，必须至少覆盖 read_write_split 或 cluster 的真实场景
3. 如果改动只影响编译期代码生成或纯模型构造，可以补充纯测试，但不能替代真实集成测试
4. 如果 CI 与本地结果不一致，优先假设是“测试前置条件缺失”，先检查 fresh 环境里的索引、数据和拓扑初始化
